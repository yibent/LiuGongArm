# 生产环境部署在不同域名时，设置完整的 WebSocket 地址。
# 本地开发无需设置，Vite 会将 /v1/stt 代理到 localhost:3000。
# VITE_BUSAGENT_WS_URL=wss://api.example.com/v1/stt

# 前端和后端分开部署时，状态接口使用这个 HTTP(S) 地址。
# VITE_BUSAGENT_HTTP_URL=https://api.example.com

# GPU 服务器的 3000 端口被占用时，可修改 Vite 本地代理目标。
# BUSAGENT_PROXY_TARGET=http://127.0.0.1:3100
